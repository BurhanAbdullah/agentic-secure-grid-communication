name: Aegis-Grid Security Audit

on: [push]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install pycryptodome

      - name: Run Layer 1-7 Verification
        run: |
          python3 -m tests.verify_v1
EOF       fipython3 .proprietary_core/tests/audit_v2.py
@BurhanAbdullah ➜ /workspaces/agentic-secure-grid-communication (main) $ # Add the badge to the top of the README
sed -i '1i ![Audit Status](https://github.com/BurhanAbdullah/Aegis-Grid/actions/workflows/security_audit.yml/badge.svg)\n' README.md

# Ensure the 'How to Reproduce' section is clean
cat >> README.md << 'EOF'

## 🔗 Live Verification
The project uses GitHub Actions for continuous security auditing. The "Audit Status" badge at the top of this page confirms that all 7 layers of the architecture (Identity to Safety) are functioning correctly in the current build.
